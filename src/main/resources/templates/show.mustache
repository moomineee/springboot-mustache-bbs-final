{{>layouts/header}}
<table class="table table-hover">
    <thead>
    <tr>
        <th scope="col">Id</th>
        <th scope="col">Title</th>
        <th scope="col">Content</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <th scope="row">{{article.id}}</th>
        <td>{{article.title}}</td>
        <td>{{article.content}}</td>
    </tr>
    </tbody>
</table>
<a class="btn btn-primary" href="/articles/{{id}}/edit">수정</a>
<a class="btn btn-danger" href="/articles/{{id}}/delete">삭제</a>
{{>layouts/footer}}